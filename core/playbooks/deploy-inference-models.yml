# Copyright (C) 2025-2026 Intel Corporation
# SPDX-License-Identifier: Apache-2.0
---
- name: Deploy/Remove Inference LLM Models
  hosts: "{{ inference_delegate | default('kube_control_plane') }}"
  gather_facts: false
  any_errors_fatal: "{{ any_errors_fatal | default(true) }}"
  environment: "{{ proxy_disable_env | default(env_proxy | default({})) }}"
  vars_files:
    - "{{ lookup('env', 'PWD') }}/config/vault.yml"
    - "{{ lookup('env', 'PWD') }}/config/vars/inference_llm_models.yml"
    - "{{ lookup('env', 'PWD') }}/config/inference_env.yml"
  roles:
    - role: inference-tools
  tasks:
    - name: Print active tags for this task
      debug:
        var: ansible_run_tags
      tags: always
      run_once: true

    - name: Display deployment configuration
      debug:
        msg:
          - "==================================================================="
          - "Inference Model Deployment Configuration"
          - "==================================================================="
          - "CPU Deployment: {{ cpu_playbook }}"
          - "NRI CPU Balloons: {{ 'ENABLED' if cpu_playbook == 'true' else 'DISABLED' }}"
          - "Models to Deploy: {{ model_name_list }}"
      tags: always
      run_once: true
      when: balloon_policy_cpu == "enabled"

    - name: Set default model parallelism configuration for CPU deployments
      ansible.builtin.set_fact:
        model_parallelism_config:
          strategy: "{{ model_parallelism_config.strategy | default('tensor_parallel') }}"
      when:
        - cpu_playbook == 'true'
        - model_parallelism_config is not defined
      tags: always
      run_once: true

    - name: Setup Environment
      block:
        - name: Create/Update Kubernetes Secret for Hugging Face Token
          kubernetes.core.k8s:
            name: hugging-face-token
            namespace: default
            kind: Secret
            definition:
              stringData:
                hugging-face-token: "{{ hugging_face_token }}"
            apply: yes
            state: present
          run_once: true
          when: hugging_face_token is defined
          tags: always
        - name: Ensure Remote Directory Exists
          ansible.builtin.file:
            path: "{{ remote_helm_charts_base }}"
            state: directory
            mode: "0755"
            owner: "{{ ansible_user }}"
            group: "{{ ansible_user }}"
          tags: always
        - name: Sync dependency files to Deployment Nodes
          ansible.posix.synchronize:
            src: "{{ item.src }}/"
            dest: "{{ item.dest }}/"
            recursive: yes
            delete: no
            mode: push
          loop:
            - {
                src: "{{ helm_charts_base }}",
                dest: "{{ remote_helm_charts_base }}/",
              }
          run_once: true
          tags: always
        - name: Use EKS-specific ingress configuration for VLLM
          ansible.builtin.copy:
            src: "{{ remote_helm_charts_base }}/vllm/templates/ingress_eks.yaml"
            dest: "{{ remote_helm_charts_base }}/vllm/templates/ingress.yaml"
            mode: "0644"
            remote_src: yes
          when: kubernetes_platform == "eks"
          run_once: true
          tags: always
        - name: Remove duplicate EKS ingress template for VLLM
          ansible.builtin.file:
            path: "{{ remote_helm_charts_base }}/vllm/templates/ingress_eks.yaml"
            state: absent
          when: kubernetes_platform == "eks"
          run_once: true
          tags: always
        - name: Remove EKS ingress template for VLLM when not on EKS
          ansible.builtin.file:
            path: "{{ remote_helm_charts_base }}/vllm/templates/ingress_eks.yaml"
            state: absent
          when: kubernetes_platform != "eks"
          run_once: true
          tags: always
        - name: Use EKS-specific ingress configuration for TGI
          ansible.builtin.copy:
            src: "{{ remote_helm_charts_base }}/tgi/templates/ingress_eks.yaml"
            dest: "{{ remote_helm_charts_base }}/tgi/templates/ingress.yaml"
            mode: "0644"
            remote_src: yes
          when: kubernetes_platform == "eks"
          run_once: true
          tags: always
        - name: Remove duplicate EKS ingress template for TGI
          ansible.builtin.file:
            path: "{{ remote_helm_charts_base }}/tgi/templates/ingress_eks.yaml"
            state: absent
          when: kubernetes_platform == "eks"
          run_once: true
          tags: always
        - name: Remove EKS ingress template for TGI when not on EKS
          ansible.builtin.file:
            path: "{{ remote_helm_charts_base }}/tgi/templates/ingress_eks.yaml"
            state: absent
          when: kubernetes_platform != "eks"
          run_once: true
          tags: always

        - name: Verify only one ingress template exists for each chart (EKS)
          ansible.builtin.shell: |
            echo "VLLM templates:"
            ls -la {{ remote_helm_charts_base }}/vllm/templates/ingress*.yaml || echo "No files found"
            echo "TGI templates:"
            ls -la {{ remote_helm_charts_base }}/tgi/templates/ingress*.yaml || echo "No files found"
          register: template_verification
          when: kubernetes_platform == "eks"
          run_once: true
          tags: always

        # OpenShift-specific tasks: Remove all ingress templates and keep only route templates
        - name: Remove standard ingress template for VLLM (OpenShift)
          ansible.builtin.file:
            path: "{{ remote_helm_charts_base }}/vllm/templates/ingress.yaml"
            state: absent
          when: kubernetes_platform == "openshift"
          run_once: true
          tags: always
        - name: Remove EKS ingress template for VLLM (OpenShift)
          ansible.builtin.file:
            path: "{{ remote_helm_charts_base }}/vllm/templates/ingress_eks.yaml"
            state: absent
          when: kubernetes_platform == "openshift"
          run_once: true
          tags: always
        - name: Remove standard ingress template for TGI (OpenShift)
          ansible.builtin.file:
            path: "{{ remote_helm_charts_base }}/tgi/templates/ingress.yaml"
            state: absent
          when: kubernetes_platform == "openshift"
          run_once: true
          tags: always
        - name: Remove EKS ingress template for TGI (OpenShift)
          ansible.builtin.file:
            path: "{{ remote_helm_charts_base }}/tgi/templates/ingress_eks.yaml"
            state: absent
          when: kubernetes_platform == "openshift"
          run_once: true
          tags: always

        - name: Display template verification results
          debug:
            msg: "{{ template_verification.stdout_lines }}"
          when:
            - kubernetes_platform == "eks"
            - template_verification is defined
          run_once: true
          tags: always
    - name: CPU Topology Detection and Optimization (for CPU deployments only)
      when: cpu_playbook == 'true' and balloon_policy_cpu == "enabled"
      run_once: true
      tags: always
      block:
        - name: Perform CPU topology detection and optimization
          ansible.builtin.include_role:
            name: utils
            tasks_from: get_optimized_cpu_topology
          vars:
            target_model: "llama-8b" # Default model for topology calculation
        - name: Calculate already-allocated CPUs from running pods
          ansible.builtin.shell: |
            kubectl get pods -A -o json 2>/dev/null | python3 -c \
              "import json,sys; data=json.load(sys.stdin); total=sum(int(c.get('resources',{}).get('requests',{}).get('cpu','0').rstrip('m'))/1000 if c.get('resources',{}).get('requests',{}).get('cpu','0').endswith('m') else float(c.get('resources',{}).get('requests',{}).get('cpu','0') or 0) for pod in data.get('items',[]) if (pod.get('status') or {}).get('phase','') in ('Running','Pending') for c in pod.get('spec',{}).get('containers',[])); print(int(total))" \
              || echo 0
          register: already_allocated_cpus_result
          run_once: true
          ignore_errors: true

        - name: Set already_allocated_cpus fact
          ansible.builtin.set_fact:
            already_allocated_cpus: "{{ already_allocated_cpus_result.stdout | default('0') | trim | int }}"
          run_once: true

        - name: Calculate CPU allocation for model deployment
          ansible.builtin.set_fact:
            calculated_topology:
              total_sockets: "{{ cpu_topology_config.total_sockets | int }}"
              numa_nodes_per_socket: "{{ cpu_topology_config.numa_nodes_per_socket | int }}"
              cpus_per_socket: "{{ cpu_topology_config.cpus_per_socket | int }}"
              is_single_node: "{{ cpu_topology_config.total_sockets == 1 and cpu_topology_config.numa_nodes_per_socket == 1 }}"
              reserved_cpus_percentage: "{{ reserved_cpus_percentage | default(18) | int }}"
              reserved_cpus: >-
                {%- set reserved_cpu_count = nri_reserved_cpu_list.split(',') | length -%}
                {{ reserved_cpu_count }}
              balloon_cpus: >-
                {%- set total_cpus = (cpu_topology_config.cpus_per_socket | int) * (cpu_topology_config.total_sockets | int) -%}
                {%- set reserved_cpu_count = nri_reserved_cpu_list.split(',') | length -%}
                {%- if reserved_cpu_count >= total_cpus -%}{%- set reserved_cpu_count = total_cpus -%}{%- endif -%}
                {{ total_cpus - reserved_cpu_count }}
              workload_cpus: >-
                {%- set computed = cpu_topology_config.workload_cpus | int -%}
                {%- set total_allocatable = (cpu_topology_config.cpus_per_socket | int) * (cpu_topology_config.total_sockets | int) -%}
                {%- set already_used = already_allocated_cpus | default(0) | int -%}
                {%- set headroom_buffer = 4 -%}
                {%- set available = total_allocatable - already_used - headroom_buffer -%}
                {%- if available < 8 -%}{%- set available = 8 -%}{%- endif -%}
                {%- if computed > available -%}
                {{ available }}
                {%- else -%}
                {{ computed }}
                {%- endif -%}

        - name: Calculate parallelism configuration
          ansible.builtin.set_fact:
            parallelism_config:
              tensor_parallel_size: >-
                {%- set total_numa = (cpu_topology_config.total_sockets | int) * (cpu_topology_config.numa_nodes_per_socket | int) -%}
                {%- if total_numa in [1, 2, 4] -%}
                {{ total_numa }}
                {%- elif total_numa in [3] -%}
                1
                {%- elif total_numa in [6] -%}
                4
                {%- else -%}
                1
                {%- endif -%}
              strategy: >-
                tensor_parallel

        - name: Combine topology and parallelism configs
          ansible.builtin.set_fact:
            optimal_balloon_config: "{{ calculated_topology | combine(parallelism_config) }}"

        - name: Display detected CPU topology for model deployment
          debug:
            msg:
              - "==================================================================="
              - "CPU Topology Detection Results for Model Deployment"
              - "==================================================================="
              - "Target Worker Node: {{ optimal_balloon_config.target_worker_node | default('auto-detected') }}"
              - "Total Sockets: {{ optimal_balloon_config.total_sockets }}"
              - "NUMA Nodes per Socket: {{ optimal_balloon_config.numa_nodes_per_socket }}"
              - "CPUs per Socket: {{ optimal_balloon_config.cpus_per_socket }}"
              - "Is Single Node: {{ optimal_balloon_config.is_single_node }}"
              - "Reserved CPUs Count: {{ optimal_balloon_config.reserved_cpus }} ({{ ((optimal_balloon_config.reserved_cpus | int / optimal_balloon_config.cpus_per_socket | int) * 100) | round(1) }}% actual)"
              - "Workload CPUs (for xeon-values.yaml): {{ optimal_balloon_config.workload_cpus }}"
              - "Balloon CPUs: {{ optimal_balloon_config.balloon_cpus }}"
              - "Tensor Parallel Size: {{ optimal_balloon_config.tensor_parallel_size | default(1) }}"
              - "Pipeline Parallel Size: {{ optimal_balloon_config.pipeline_parallel_size | default(1) }}"
              - "Strategy: {{ optimal_balloon_config.strategy }}"
              - "==================================================================="

        - name: Check if vLLM helm charts directory exists
          ansible.builtin.stat:
            path: "{{ remote_helm_charts_base }}/vllm"
          register: vllm_charts_dir

        - name: Display helm charts path status
          debug:
            msg:
              - "Helm charts path: {{ remote_helm_charts_base }}/vllm"
              - "Directory exists: {{ vllm_charts_dir.stat.exists }}"

        - name: Warning when vLLM helm charts not found
          debug:
            msg:
              - "==================================================================="
              - "WARNING: vLLM Helm Charts Directory Not Found"
              - "==================================================================="
              - "Expected path: {{ remote_helm_charts_base }}/vllm/xeon-values.yaml"
              - "This means vLLM chart values cannot be automatically updated"
              - "Model deployment will continue but may not be optimized"
              - "==================================================================="
          when: not vllm_charts_dir.stat.exists

        - name: Extract sockets memory
          ansible.builtin.set_fact:
            usable_mem_var: "{{ cpu_topology_config.usable_memory_per_socket }}"
            socket0_memory_gb: 0

        - name: Extract socket0 memory
          ansible.builtin.set_fact:
            socket0_memory_gb: "{{ usable_mem_var[0] | int }}"
          when: usable_mem_var is mapping and socket0_memory_gb | int == 0 and 0 in usable_mem_var
          ignore_errors: true

        - name: Fallback to parsing from string if dictionary access failed
          ansible.builtin.set_fact:
            socket0_memory_gb: "{{ usable_mem_var | string | regex_search('0: (\\d+)') | default(['0', '0']) | last | int }}"
          when: usable_mem_var is string and socket0_memory_gb | int == 0
          ignore_errors: true

        - name: Calculate minimum memory based on tensor parallel size
          ansible.builtin.set_fact:
            min_memory_for_tp: >-
              {%- set tp = optimal_balloon_config.tensor_parallel_size | default(1) | int -%}
              {%- if tp >= 4 -%}
              256
              {%- elif tp >= 2 -%}
              128
              {%- else -%}
              128
              {%- endif -%}

        - name: Set optimal memory based on TP requirements and available memory
          ansible.builtin.set_fact:
            optimal_memory_gb: >-
              {%- set available = socket0_memory_gb | int -%}
              {%- set min_required = min_memory_for_tp | int -%}
              {%- set max_limit = 256 -%}
              {%- if available < min_required -%}
              {{ min_required }}
              {%- elif available > max_limit -%}
              {{ max_limit }}
              {%- else -%}
              {{ available }}
              {%- endif -%}

        - name: Final memory calculation
          ansible.builtin.debug:
            msg: |
              === Memory Calculation Summary ===
              Tensor Parallel Size: {{ optimal_balloon_config.tensor_parallel_size | default(1) }}
              Minimum memory required for TP: {{ min_memory_for_tp }} GB
              Socket 0 memory available: {{ socket0_memory_gb }} GB
              Final memory for deployment: {{ optimal_memory_gb }} GB
              Memory allocation: {{ 'Using minimum required' if (socket0_memory_gb | int) < (min_memory_for_tp | int) else 'Using available memory' if (socket0_memory_gb | int) <= 256 else 'Capped at 256GB max' }}

        - name: Apply optimized TP/PP defaults (unless user overrides)
          ansible.builtin.set_fact:
            # Cap tensor_parallel_size at 1 for single-node CPU deployments.
            # Multi-NUMA auto-detection (e.g. 2 NUMA nodes → TP=2) causes an
            # asymmetric-NUMA crash on Xeon (shm.cpp thread_num mismatch).
            # TP > 1 requires symmetric NUMA + explicit user override via
            # huggingface_tensor_parellel_size in agentic-config.cfg.
            tensor_parallel_size: "{{ [tensor_parallel_size | default(optimal_balloon_config.get('tensor_parallel_size', 1)) | int, 1] | min }}"
            pipeline_parallel_size: "{{ pipeline_parallel_size | default(optimal_balloon_config.get('pipeline_parallel_size', 1)) | int }}"
          when:
            - cpu_playbook == 'true'
          tags: always
          run_once: true

        - name: Display CPU optimization helm charts update completion
          debug:
            msg:
              - "==================================================================="
              - "CPU Optimization Configuration Complete"
              - "==================================================================="
              - "✓ CPU topology detected and analyzed"
              - "✓ Memory calculated ({{ optimal_memory_gb | default('8') }}Gi based on available socket memory)"
              - "✓ Optimized values prepared for helm deployment"
              - "✓ CPU: {{ optimal_balloon_config.workload_cpus | default(8) }} cores"
              - "✓ Memory: {{ optimal_memory_gb | default('8') }}Gi"
              - "✓ Tensor Parallel Size: {{ optimal_balloon_config.tensor_parallel_size | default(1) }}"
              - "✓ Pipeline Parallel Size: {{ optimal_balloon_config.pipeline_parallel_size | default(1) }}"
              - "✓ Values will be set via --set flags during helm install"
              - "✓ xeon-values.yaml remains unchanged (template-based)"
              - "✓ Ready for model deployment with NRI balloon policy"
              - "==================================================================="

    - name: Setup Environment (continued)
      block:
        - name: Transfer Dependency keycloak-realmcreationfile
          ansible.builtin.copy:
            src: "{{ remote_home_dir }}/"
            dest: "{{ remote_helm_charts_base }}/"
            mode: "0755"
            owner: "{{ ansible_user }}"
            group: "{{ ansible_user }}"
            remote_src: no
          tags: install-keycloak-apisix
        - name: Ensure the script is executable
          file:
            path: "{{ remote_helm_charts_base }}/keycloak-realmcreation.sh"
            mode: "0755"
            owner: "{{ ansible_user }}"
            group: "{{ ansible_user }}"
          tags: install-keycloak-apisix
        - name: Update Dependencies for Auth-Apisix on Nodes
          ansible.builtin.command: helm dependency update "{{ remote_helm_charts_base }}/apisix-helm/"
          register: helm_dependency_update
          failed_when: helm_dependency_update.rc != 0
          when: apisix_enabled == "yes"
          tags: install-keycloak-apisix
    - name: Fetch the keycloak client secret
      block:
        - name: Create ConfigMap with Keycloak realm creation script
          kubernetes.core.k8s:
            state: present
            definition:
              apiVersion: v1
              kind: ConfigMap
              metadata:
                name: keycloak-realm-script
                namespace: default
              data:
                keycloak-realmcreation.sh: "{{ lookup('file', remote_home_dir + '/keycloak-realmcreation.sh') }}"
          run_once: true

        - name: Create Keycloak realm setup Job
          kubernetes.core.k8s:
            state: present
            definition:
              apiVersion: batch/v1
              kind: Job
              metadata:
                name: keycloak-realm-setup-models
                namespace: default
              spec:
                ttlSecondsAfterFinished: 300
                backoffLimit: 3
                template:
                  metadata:
                    name: keycloak-realm-setup
                  spec:
                    restartPolicy: Never
                    containers:
                    - name: keycloak-setup
                      image: ubuntu:22.04
                      env: >-
                        {{
                          [
                            {'name': 'http_proxy',  'value': env_proxy.http_proxy  | default('')},
                            {'name': 'https_proxy', 'value': env_proxy.https_proxy | default('')},
                            {'name': 'HTTP_PROXY',  'value': env_proxy.http_proxy  | default('')},
                            {'name': 'HTTPS_PROXY', 'value': env_proxy.https_proxy | default('')},
                            {'name': 'no_proxy',    'value': env_proxy.no_proxy    | default('')},
                            {'name': 'NO_PROXY',    'value': env_proxy.no_proxy    | default('')}
                          ] if (env_proxy is defined and env_proxy.http_proxy | default('') != '') else []
                        }}
                      command: ["/bin/bash", "-c"]
                      args:
                        - |
                          set -e

                          # Install dependencies
                          export DEBIAN_FRONTEND=noninteractive
                          apt-get update -qq
                          apt-get install -y -qq curl jq

                          # Copy script to writable location
                          cp /scripts/keycloak-realmcreation.sh /tmp/keycloak-realmcreation.sh
                          chmod +x /tmp/keycloak-realmcreation.sh

                          # Wait for Keycloak to be ready with retry logic
                          MAX_RETRIES=30
                          RETRY_DELAY=10
                          RETRY_COUNT=0

                          echo "Waiting for Keycloak to be ready..."
                          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
                            echo "Attempt $((RETRY_COUNT + 1))/$MAX_RETRIES: Testing Keycloak authentication..."

                            HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
                              -X POST \
                              -d "client_id=admin-cli" \
                              -d "username={{ keycloak_admin_user }}" \
                              -d "password={{ keycloak_admin_password }}" \
                              -d "grant_type=password" \
                              http://keycloak.default.svc.cluster.local:80/realms/master/protocol/openid-connect/token)

                            if [ "$HTTP_CODE" = "200" ]; then
                              echo "Keycloak is ready and authenticated successfully"
                              break
                            else
                              echo "Keycloak not ready yet (HTTP $HTTP_CODE), waiting ${RETRY_DELAY}s..."
                              RETRY_COUNT=$((RETRY_COUNT + 1))
                              if [ $RETRY_COUNT -lt $MAX_RETRIES ]; then
                                sleep $RETRY_DELAY
                              fi
                            fi
                          done

                          if [ $RETRY_COUNT -eq $MAX_RETRIES ]; then
                            echo "ERROR: Keycloak did not become ready after $MAX_RETRIES attempts"
                            exit 1
                          fi

                          # Additional stabilization wait
                          echo "Waiting 15 seconds for Keycloak to stabilize..."
                          sleep 15

                          # Run the realm creation script
                          echo "Running Keycloak realm creation script..."
                          /tmp/keycloak-realmcreation.sh \
                            keycloak.default.svc.cluster.local:80 \
                            "{{ keycloak_admin_user }}" \
                            "{{ keycloak_admin_password }}" \
                            "{{ keycloak_client_id }}"
                      volumeMounts:
                      - name: scripts
                        mountPath: /scripts
                        readOnly: true
                    volumes:
                    - name: scripts
                      configMap:
                        name: keycloak-realm-script
                        defaultMode: 0755
                    tolerations:
                      - key: node-role.kubernetes.io/control-plane
                        operator: Exists
                        effect: NoSchedule
                      - key: node-role.kubernetes.io/master
                        operator: Exists
                        effect: NoSchedule
          run_once: true

        - name: Wait for Keycloak realm setup Job to complete
          kubernetes.core.k8s_info:
            kind: Job
            namespace: default
            name: keycloak-realm-setup-models
          register: realm_job
          until: >
            realm_job.resources[0].status.succeeded is defined and
            realm_job.resources[0].status.succeeded == 1
          retries: 30
          delay: 10
          run_once: true

        - name: Get Keycloak realm setup Job logs
          command: kubectl logs job/keycloak-realm-setup-models -n default
          register: script_output
          run_once: true

        - name: Set Keycloak client fact
          set_fact:
            client_secret: "{{ script_output.stdout | regex_search('Client secret: (.*)') | join('') | regex_replace('^Client secret: ') }}"
          when: script_output.stdout is search('Client secret:')
          run_once: true

        - name: Warning when client secret not found
          debug:
            msg: "WARNING: Client secret was not found in the Keycloak setup job output. Please check the job logs manually."
          when: client_secret is not defined
          run_once: true
      run_once: true
      when: deploy_keycloak == 'yes'
      tags: install-keycloak-apisix
    - name: env_proxy Contents
      debug:
        var: env_proxy
      tags: always
    - name: Set proxy args if proxy is defined
      set_fact:
        helm_proxy_args: >-
          {% if env_proxy is defined and env_proxy.http_proxy | default('') != '' %}
          --set global.http_proxy="{{ env_proxy.http_proxy | replace(',', '\\,') }}"
          {% if env_proxy.https_proxy | default('') != '' %}
          --set global.https_proxy="{{ env_proxy.https_proxy | replace(',', '\\,') }}"
          {% endif %}
          {% if env_proxy.no_proxy | default('') != '' %}
          --set global.no_proxy="{{ env_proxy.no_proxy | replace(',', '\\,') }}"
          {% endif %}
          {% else %}
          {{ '' }}
          {% endif %}
      tags: always
    - name: Print model_name_list
      debug:
        var: model_name_list
      tags: always
      run_once: true
    - name: Check if CPU Model is installed
      ansible.builtin.command:
        cmd: "helm list --filter vllm-llama-8b-cpu --short"
      register: helm_release_installed
      ignore_errors: true
      run_once: true
      tags: uninstall-cpu-llama-8b
    - name: Uninstall CPU  Model
      ansible.builtin.command:
        cmd: "helm uninstall vllm-llama-8b-cpu"
      run_once: true
      tags: uninstall-cpu-llama-8b
      when:
        - model_name_list is defined
        - "'cpu-llama-8b' in (model_name_list | regex_replace(',', ' ') | split())"
        - uninstall_true == 'true'
        - helm_release_installed.stdout != ""


    - name: Deploy CPU based LLM model  llama 8b
      block:
        - name: Delete Ingress resource Llama8b from default namespace
          tags: install-cpu-llama-8b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: default
            name: vllm-llama-8b-cpu-ingress
            state: absent
          ignore_errors: yes
        - name: Delete Ingress resource Llama8b from auth-apisix namespace
          tags: install-cpu-llama-8b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: auth-apisix
            name: vllm-llama-8b-cpu-ingress
            state: absent
          ignore_errors: yes
        - name: Deploy CPU based LLM model  llama 8b Installation
          ansible.builtin.command: >-
            helm upgrade --install vllm-llama-8b-cpu "{{ remote_helm_charts_base }}/vllm"
            --values "{{ xeon_values_file }}"
            --set LLM_MODEL_ID="meta-llama/Llama-3.1-8B-Instruct"
            --set global.monitoring="{{ vllm_metrics_enabled }}"
            --set svcmonitor.enabled="{{ vllm_metrics_enabled }}"
            --set global.HUGGINGFACEHUB_API_TOKEN={{ hugging_face_token }}
            {% if cpu_playbook == 'true' %}
            --set cpu_balloon_annotation="vllm-balloon"
            --set podLabels.name="vllm"
            --set cpu="{{ optimal_balloon_config.workload_cpus | default(8) }}"
            --set memory="{{ optimal_memory_gb | default(8) }}Gi"
            --set tensor_parallel_size={{ tensor_parallel_size | default(1) }}
            --set pipeline_parallel_size={{ pipeline_parallel_size | default(1) }}
            {% endif %}
            {% if apisix_enabled %}
              --set apisix.enabled={{ apisix_enabled }}
              --set platform={{ kubernetes_platform }}
            {% endif %}
            {% if kubernetes_platform == 'openshift' %}
              --set route.enabled=true
              --set route.host={{ secret_name }}
              --set route.tls.termination=edge
              --set route.tls.insecureEdgeTerminationPolicy=Redirect
              --set ingress.enabled=false
            {% elif ingress_enabled %}
              --set ingress.enabled={{ ingress_enabled }}
              --set ingress.host={{ secret_name }}
              --set ingress.secretname={{ secret_name }}
              --set route.enabled=false
              {% if kubernetes_platform == "eks" %}
              --set aws_certificate_arn={{ aws_certificate_arn | default('') }}
              {% endif %}
            {% endif %}
            {% if deploy_keycloak == 'yes' and apisix_enabled %}
              --set oidc.client_id={{ keycloak_client_id | default('') }}
              --set oidc.client_secret={{ client_secret | default('') }}
            {% endif %}
            {{ helm_proxy_args | default('') }}
          register: helm_upgrade_install_model_deployment_cpu_llama8b
          failed_when: helm_upgrade_install_model_deployment_cpu_llama8b.rc != 0
        - name: Register Xeon/CPU llama 8b model
          import_tasks: register-model-genai-gateway.yml
          vars:
            reg_model_name: "meta-llama/Llama-3.1-8B-Instruct"
            reg_litellm_model: "openai/meta-llama/Llama-3.1-8B-Instruct"
            reg_custom_llm_provider: "openai"
            reg_api_base: "http://vllm-llama-8b-cpu-service.default/v1"
            reg_input_cost_per_token: 0.001
            reg_output_cost_per_token: 0.002
          tags:
            - install-cpu-llama-8b
            - install-genai-gateway
          run_once: true
          when:
            - "'install-cpu-llama-8b' in ansible_run_tags"
            - "'install-genai-gateway' in ansible_run_tags"
      run_once: true
      tags: install-cpu-llama-8b
      when:
        - cpu_playbook == 'true'
        - install_true == 'true'
        - model_name_list is defined
        - "'cpu-llama-8b' in (model_name_list | regex_replace(',', ' ') | split())"

    - name: Check if CPU Deepseek R1 Distill Qwen 32b is installed
      ansible.builtin.command:
        cmd: "helm list --filter vllm-deepseek-r1-qwen32b-cpu --short"
      register: helm_release_installed
      ignore_errors: true
      run_once: true
      tags: uninstall-cpu-deepseek-r1-distill-qwen-32b
    - name: Uninstall Deepseek R1 Distill Qwen 32b
      ansible.builtin.command:
        cmd: "helm uninstall vllm-deepseek-r1-qwen32b-cpu"
      run_once: true
      tags: uninstall-cpu-deepseek-r1-distill-qwen-32b
      when:
        - model_name_list is defined
        - "'cpu-deepseek-r1-distill-qwen-32b' in (model_name_list | regex_replace(',', ' ') | split())"
        - uninstall_true == 'true'
        - helm_release_installed.stdout != ""

    - name: Deploy CPU Deepseek R1 Distill Qwen 32b LLM model
      block:
        - name: Delete Ingress resource  Deepseek R1 Distill Qwen 32b from default namespace
          tags: install-cpu-deepseek-r1-distill-qwen-32b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: default
            name: vllm-deepseek-r1-qwen32b-cpu-ingress
            state: absent
        - name: Delete Ingress resource  Deepseek R1 Distill Qwen 32b from auth-apisix namespace
          tags: install-cpu-deepseek-r1-distill-qwen-32b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: auth-apisix
            name: vllm-deepseek-r1-qwen32b-cpu-ingress
            state: absent
        - name: Deploy CPU based LLM model Deepseek R1 Distill Qwen 32b Installation
          ansible.builtin.command: >-
            helm upgrade --install vllm-deepseek-r1-qwen32b-cpu "{{ remote_helm_charts_base }}/vllm"
            --values "{{ xeon_values_file }}"
            --set LLM_MODEL_ID="deepseek-ai/DeepSeek-R1-Distill-Qwen-32B"
            --set global.monitoring="{{ vllm_metrics_enabled }}"
            --set svcmonitor.enabled="{{ vllm_metrics_enabled }}"
            --set global.HUGGINGFACEHUB_API_TOKEN={{ hugging_face_token }}
            {% if cpu_playbook == 'true' %}
            --set cpu_balloon_annotation="vllm-balloon"
            --set podLabels.name="vllm"
            --set cpu="{{ optimal_balloon_config.workload_cpus | default(8) }}"
            --set memory="{{ optimal_memory_gb | default(8) }}Gi"
            --set tensor_parallel_size={{ model_paralletensor_parallel_size | default(1) }}
            --set pipeline_parallel_size={{ pipeline_parallel_size | default(1) }}
            {% endif %}
            {% if apisix_enabled %}
              --set apisix.enabled={{ apisix_enabled }}
              --set platform={{ kubernetes_platform }}
            {% endif %}
            {% if kubernetes_platform == 'openshift' %}
              --set route.enabled=true
              --set route.host={{ secret_name }}
              --set route.tls.termination=edge
              --set route.tls.insecureEdgeTerminationPolicy=Redirect
              --set ingress.enabled=false
            {% elif ingress_enabled %}
              --set ingress.enabled={{ ingress_enabled }}
              --set ingress.host={{ secret_name }}
              --set ingress.secretname={{ secret_name }}
              --set route.enabled=false
              {% if kubernetes_platform == "eks" %}
              --set aws_certificate_arn={{ aws_certificate_arn | default('') }}
              {% endif %}
            {% endif %}
            {% if deploy_keycloak == 'yes' and apisix_enabled %}
              --set oidc.client_id={{ keycloak_client_id | default('') }}
              --set oidc.client_secret={{ client_secret | default('') }}
            {% endif %}
            {{ helm_proxy_args | default('') }}
          register: helm_upgrade_install_model_deployment_cpu_deepseek_qwen32b
          failed_when: helm_upgrade_install_model_deployment_cpu_deepseek_qwen32b.rc != 0
        - name: Register Xeon/CPU Deepseek R1 Distill Qwen 32b model
          import_tasks: register-model-genai-gateway.yml
          vars:
            reg_model_name: "deepseek-ai/DeepSeek-R1-Distill-Qwen-32B"
            reg_litellm_model: "openai/deepseek-ai/DeepSeek-R1-Distill-Qwen-32B"
            reg_custom_llm_provider: "openai"
            reg_api_base: "http://vllm-deepseek-r1-qwen32b-cpu-service.default/v1"
            reg_input_cost_per_token: 0.001
            reg_output_cost_per_token: 0.002
          tags:
            - install-cpu-deepseek-r1-distill-qwen-32b
            - install-genai-gateway
          run_once: true
          when:
            - "'install-cpu-deepseek-r1-distill-qwen-32b' in ansible_run_tags"
            - "'install-genai-gateway' in ansible_run_tags"
      run_once: true
      tags: install-cpu-deepseek-r1-distill-qwen-32b
      when:
        - model_name_list is defined
        - cpu_playbook == 'true'
        - install_true == 'true'
        - "'cpu-deepseek-r1-distill-qwen-32b' in (model_name_list | regex_replace(',', ' ') | split())"
    
    - name: Check if CPU Deepseek R1 Distill Llama 8B is installed
      ansible.builtin.command:
        cmd: "helm list --filter vllm-deepseek-r1-llama-8b-cpu --short"
      register: helm_release_installed
      ignore_errors: true
      run_once: true
      tags: uninstall-cpu-deepseek-r1-distill-llama8b
    - name: Uninstall Deepseek R1 Distill Llama 8B
      ansible.builtin.command:
        cmd: "helm uninstall vllm-deepseek-r1-llama-8b-cpu"
      run_once: true
      tags: uninstall-cpu-deepseek-r1-distill-llama8b
      when:
        - "'cpu-deepseek-r1-distill-llama8b' in (model_name_list | regex_replace(',', ' ') | split())"
        - uninstall_true == 'true'
        - helm_release_installed.stdout != ""

    - name: Deploy CPU Deepseek R1 Distill Llama 8B LLM model
      block:
        - name: Delete Ingress resource Deepseek R1 Distill Llama 8B from default namespace
          tags: install-cpu-deepseek-r1-distill-llama8b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: default
            name: vllm-deepseek-r1-llama-8b-cpu-ingress
            state: absent
        - name: Delete Ingress resource Deepseek R1 Distill Llama 8B from auth-apisix namespace
          tags: install-cpu-deepseek-r1-distill-llama8b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: auth-apisix
            name: vllm-deepseek-r1-llama-8b-cpu-ingress
            state: absent
        - name: Deploy CPU based LLM model Deepseek R1 Distill Llama 8B Installation
          ansible.builtin.command: >-
            helm upgrade --install vllm-deepseek-r1-llama-8b-cpu "{{ remote_helm_charts_base }}/vllm"
            --values "{{ xeon_values_file }}"
            --set LLM_MODEL_ID="deepseek-ai/DeepSeek-R1-Distill-Llama-8B"
            --set global.monitoring="{{ vllm_metrics_enabled }}"
            --set svcmonitor.enabled="{{ vllm_metrics_enabled }}"
            --set global.HUGGINGFACEHUB_API_TOKEN={{ hugging_face_token }}
            {% if cpu_playbook == 'true' %}
            --set cpu_balloon_annotation="vllm-balloon"
            --set podLabels.name="vllm"
            --set cpu="{{ optimal_balloon_config.workload_cpus | default(8) }}"
            --set memory="{{ optimal_memory_gb | default(8) }}Gi"
            --set tensor_parallel_size={{ tensor_parallel_size | default(1) }}
            --set pipeline_parallel_size={{ pipeline_parallel_size | default(1) }}
            {% endif %}
            {% if apisix_enabled %}
              --set apisix.enabled={{ apisix_enabled }}
              --set platform={{ kubernetes_platform }}
            {% endif %}
            {% if kubernetes_platform == 'openshift' %}
              --set route.enabled=true
              --set route.host={{ secret_name }}
              --set route.tls.termination=edge
              --set route.tls.insecureEdgeTerminationPolicy=Redirect
              --set ingress.enabled=false
            {% elif ingress_enabled %}
              --set ingress.enabled={{ ingress_enabled }}
              --set ingress.host={{ secret_name }}
              --set ingress.secretname={{ secret_name }}
              --set route.enabled=false
              {% if kubernetes_platform == "eks" %}
              --set aws_certificate_arn={{ aws_certificate_arn | default('') }}
              {% endif %}
            {% endif %}
            {% if deploy_keycloak == 'yes' and apisix_enabled %}
              --set oidc.client_id={{ keycloak_client_id | default('') }}
              --set oidc.client_secret={{ client_secret | default('') }}
            {% endif %}
            {{ helm_proxy_args | default('') }}
          register: helm_upgrade_install_model_deployment_cpu_deepseek_llama_8b
          failed_when: helm_upgrade_install_model_deployment_cpu_deepseek_llama_8b.rc != 0
        - name: Register Xeon/CPU Deepseek R1 Distill Llama 8B model
          import_tasks: register-model-genai-gateway.yml
          vars:
            reg_model_name: "deepseek-ai/DeepSeek-R1-Distill-Llama-8B"
            reg_litellm_model: "openai/deepseek-ai/DeepSeek-R1-Distill-Llama-8B"
            reg_custom_llm_provider: "openai"
            reg_api_base: "http://vllm-deepseek-r1-llama-8b-cpu-service.default/v1"
            reg_input_cost_per_token: 0.001
            reg_output_cost_per_token: 0.002
          tags:
            - install-cpu-deepseek-r1-distill-llama8b
            - install-genai-gateway
          run_once: true
          when:
            - "'install-cpu-deepseek-r1-distill-llama8b' in ansible_run_tags"
            - "'install-genai-gateway' in ansible_run_tags"
      run_once: true
      tags: install-cpu-deepseek-r1-distill-llama8b
      when:
        - cpu_playbook == 'true'
        - install_true == 'true'
        - "'cpu-deepseek-r1-distill-llama8b' in (model_name_list | regex_replace(',', ' ') | split())"




    - name: Check if CPU Llama-3.2-3B is installed
      ansible.builtin.command:
        cmd: "helm list --filter vllm-llama-3-2-3b-cpu --short"
      register: helm_release_installed
      ignore_errors: true
      run_once: true
      tags: uninstall-cpu-llama-3-2-3b
    - name: Uninstall Llama-3.2-3B
      ansible.builtin.command:
        cmd: "helm uninstall vllm-llama-3-2-3b-cpu"
      run_once: true
      tags: uninstall-cpu-llama-3-2-3b
      when:
        - model_name_list is defined
        - "'cpu-llama-3-2-3b' in (model_name_list | regex_replace(',', ' ') | split())"
        - uninstall_true == 'true'
        - helm_release_installed.stdout != ""

    - name: Deploy CPU Llama-3.2-3B LLM model
      block:
        - name: Delete Ingress resource Llama-3.2-3B from default namespace
          tags: install-cpu-llama-3-2-3b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: default
            name: vllm-llama-3-2-3b-cpu-ingress
            state: absent
        - name: Delete Ingress resource Llama-3.2-3B from auth-apisix namespace
          tags: install-cpu-llama-3-2-3b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: auth-apisix
            name: vllm-llama-3-2-3b-cpu-ingress
            state: absent
        - name: Deploy CPU based LLM model Llama-3.2-3B Installation
          ansible.builtin.command: >-
            helm upgrade --install vllm-llama-3-2-3b-cpu "{{ remote_helm_charts_base }}/vllm"
            --values "{{ xeon_values_file }}"
            --set LLM_MODEL_ID="meta-llama/Llama-3.2-3B-Instruct"
            --set global.monitoring="{{ vllm_metrics_enabled }}"
            --set svcmonitor.enabled="{{ vllm_metrics_enabled }}"
            --set global.HUGGINGFACEHUB_API_TOKEN={{ hugging_face_token }}
            {% if cpu_playbook == 'true' %}
            --set cpu_balloon_annotation="vllm-balloon"
            --set podLabels.name="vllm"
            --set cpu="{{ optimal_balloon_config.workload_cpus | default(8) }}"
            --set memory="{{ optimal_memory_gb | default(8) }}Gi"
            --set tensor_parallel_size={{ tensor_parallel_size | default(1) }}
            --set pipeline_parallel_size={{ pipeline_parallel_size | default(1) }}
            {% endif %}
            {% if apisix_enabled %}
              --set apisix.enabled={{ apisix_enabled }}
              --set platform={{ kubernetes_platform }}
            {% endif %}
            {% if kubernetes_platform == 'openshift' %}
              --set route.enabled=true
              --set route.host={{ secret_name }}
              --set route.tls.termination=edge
              --set route.tls.insecureEdgeTerminationPolicy=Redirect
              --set ingress.enabled=false
            {% elif ingress_enabled %}
              --set ingress.enabled={{ ingress_enabled }}
              --set ingress.host={{ secret_name }}
              --set ingress.secretname={{ secret_name }}
              --set route.enabled=false
              {% if kubernetes_platform == "eks" %}
              --set aws_certificate_arn={{ aws_certificate_arn | default('') }}
              {% endif %}
            {% endif %}
            {% if deploy_keycloak == 'yes' and apisix_enabled %}
              --set oidc.client_id={{ keycloak_client_id | default('') }}
              --set oidc.client_secret={{ client_secret | default('') }}
            {% endif %}
            {{ helm_proxy_args | default('') }}
          register: helm_upgrade_install_model_deployment_cpu_llama3_2_3b_cpu
          failed_when: helm_upgrade_install_model_deployment_cpu_llama3_2_3b_cpu.rc != 0
        - name: Register Xeon/CPU Llama-3.2-3B model
          import_tasks: register-model-genai-gateway.yml
          vars:
            reg_model_name: "meta-llama/Llama-3.2-3B-Instruct"
            reg_litellm_model: "openai/meta-llama/Llama-3.2-3B-Instruct"
            reg_custom_llm_provider: "openai"
            reg_api_base: "http://vllm-llama-3-2-3b-cpu-service.default/v1"
            reg_input_cost_per_token: 0.001
            reg_output_cost_per_token: 0.002
          tags:
            - install-cpu-llama-3-2-3b
            - install-genai-gateway
          run_once: true
          when:
            - "'install-cpu-llama-3-2-3b' in ansible_run_tags"
            - "'install-genai-gateway' in ansible_run_tags"
      run_once: true
      tags: install-cpu-llama-3-2-3b
      when:
        - model_name_list is defined
        - cpu_playbook == 'true'
        - install_true == 'true'
        - "'cpu-llama-3-2-3b' in (model_name_list | regex_replace(',', ' ') | split())"








    - name: Check if CPU Qwen-3-1-7B is installed
      ansible.builtin.command:
        cmd: "helm list --filter vllm-qwen-3-1-7b-cpu --short"
      register: helm_release_installed
      ignore_errors: true
      run_once: true
      tags: uninstall-cpu-qwen3-1-7b
    - name: Uninstall Qwen-3-1-7B
      ansible.builtin.command:
        cmd: "helm uninstall vllm-qwen-3-1-7b-cpu"
      run_once: true
      tags: uninstall-cpu-qwen3-1-7b
      when:
        - model_name_list is defined
        - "'cpu-qwen3-1-7b' in (model_name_list | regex_replace(',', ' ') | split())"
        - uninstall_true == 'true'
        - helm_release_installed.stdout != ""

    - name: Deploy CPU  Qwen-3-1-7B LLM model
      block:
        - name: Delete Ingress resource Qwen-3-1-7B from default namespace
          tags: install-cpu-qwen3-1-7b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: default
            name: vllm-qwen-3-1-7b-cpu-ingress
            state: absent
        - name: Delete Ingress resource  Qwen-3-1-7B from auth-apisix namespace
          tags: install-cpu-qwen3-1-7b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: auth-apisix
            name: vllm-qwen-3-1-7b-cpu-ingress
            state: absent
        - name: Deploy CPU based LLM model Qwen-3-1-7B Installation
          ansible.builtin.command: >-
            helm upgrade --install vllm-qwen-3-1-7b-cpu "{{ remote_helm_charts_base }}/vllm"
            --values "{{ xeon_values_file }}"
            --set LLM_MODEL_ID="Qwen/Qwen3-1.7B"
            --set global.monitoring="{{ vllm_metrics_enabled }}"
            --set svcmonitor.enabled="{{ vllm_metrics_enabled }}"
            --set global.HUGGINGFACEHUB_API_TOKEN={{ hugging_face_token }}
            {% if cpu_playbook == 'true' %}
            --set cpu_balloon_annotation="vllm-balloon"
            --set podLabels.name="vllm"
            --set cpu="{{ optimal_balloon_config.workload_cpus | default(8) }}"
            --set memory="{{ optimal_memory_gb | default(8) }}Gi"
            --set tensor_parallel_size={{ tensor_parallel_size | default(1) }}
            --set pipeline_parallel_size={{ pipeline_parallel_size | default(1) }}
            {% endif %}
            {% if apisix_enabled %}
              --set apisix.enabled={{ apisix_enabled }}
              --set platform={{ kubernetes_platform }}
            {% endif %}
            {% if kubernetes_platform == 'openshift' %}
              --set route.enabled=true
              --set route.host={{ secret_name }}
              --set route.tls.termination=edge
              --set route.tls.insecureEdgeTerminationPolicy=Redirect
              --set ingress.enabled=false
            {% elif ingress_enabled %}
              --set ingress.enabled={{ ingress_enabled }}
              --set ingress.host={{ secret_name }}
              --set ingress.secretname={{ secret_name }}
              --set route.enabled=false
              {% if kubernetes_platform == "eks" %}
              --set aws_certificate_arn={{ aws_certificate_arn | default('') }}
              {% endif %}
            {% endif %}
            {% if deploy_keycloak == 'yes' and apisix_enabled %}
              --set oidc.client_id={{ keycloak_client_id | default('') }}
              --set oidc.client_secret={{ client_secret | default('') }}
            {% endif %}
            {{ helm_proxy_args | default('') }}
          register: helm_upgrade_install_model_deployment_cpu_llama3_2_3b_cpu
          failed_when: helm_upgrade_install_model_deployment_cpu_llama3_2_3b_cpu.rc != 0
        - name: Register Xeon/CPU Qwen-3-1-7B model
          import_tasks: register-model-genai-gateway.yml
          vars:
            reg_model_name: "Qwen/Qwen3-1.7B"
            reg_litellm_model: "openai/Qwen/Qwen3-1.7B"
            reg_custom_llm_provider: "openai"
            reg_api_base: "http://vllm-qwen-3-1-7b-cpu-service.default/v1"
            reg_input_cost_per_token: 0.001
            reg_output_cost_per_token: 0.002
          tags:
            - install-cpu-qwen3-1-7b
            - install-genai-gateway
          run_once: true
          when:
            - "'install-cpu-qwen3-1-7b' in ansible_run_tags"
            - "'install-genai-gateway' in ansible_run_tags"
      run_once: true
      tags: install-cpu-qwen3-1-7b
      when:
        - model_name_list is defined
        - cpu_playbook == 'true'
        - install_true == 'true'
        - "'cpu-qwen3-1-7b' in (model_name_list | regex_replace(',', ' ') | split())"



    - name: Check if CPU Qwen-3-4B is installed
      ansible.builtin.command:
        cmd: "helm list --filter vllm-qwen-3-4b-cpu --short"
      register: helm_release_installed
      ignore_errors: true
      run_once: true
      tags: uninstall-cpu-qwen3-4b
    - name: Uninstall Qwen-3-4B
      ansible.builtin.command:
        cmd: "helm uninstall vllm-qwen-3-4b-cpu"
      run_once: true
      tags: uninstall-cpu-qwen3-4b
      when:
        - model_name_list is defined
        - "'cpu-qwen3-4b' in (model_name_list | regex_replace(',', ' ') | split())"
        - uninstall_true == 'true'
        - helm_release_installed.stdout != ""

    - name: Deploy CPU  Qwen-3-4B LLM model
      block:
        - name: Delete Ingress resource Qwen-3-4B from default namespace
          tags: install-cpu-qwen3-4b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: default
            name: vllm-qwen-3-4b-cpu-ingress
            state: absent
        - name: Delete Ingress resource   Qwen-3-4B from auth-apisix namespace
          tags: install-cpu-qwen3-4b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: auth-apisix
            name: vllm-cpu-qwen-3-4b-cpu-ingress
            state: absent
        - name: Deploy CPU based LLM model  Qwen-3-4B Installation
          ansible.builtin.command: >-
            helm upgrade --install vllm-qwen-3-4b-cpu "{{ remote_helm_charts_base }}/vllm"
            --values "{{ xeon_values_file }}"
            --set LLM_MODEL_ID="Qwen/Qwen3-4B-Instruct-2507"
            --set global.monitoring="{{ vllm_metrics_enabled }}"
            --set svcmonitor.enabled="{{ vllm_metrics_enabled }}"
            --set global.HUGGINGFACEHUB_API_TOKEN={{ hugging_face_token }}
            {% if cpu_playbook == 'true' %}
            --set cpu_balloon_annotation="vllm-balloon"
            --set podLabels.name="vllm"
            --set cpu="{{ optimal_balloon_config.workload_cpus | default(8) }}"
            --set memory="{{ optimal_memory_gb | default(8) }}Gi"
            --set tensor_parallel_size={{ tensor_parallel_size | default(1) }}
            --set pipeline_parallel_size={{ pipeline_parallel_size | default(1) }}
            {% endif %}
            {% if apisix_enabled %}
              --set apisix.enabled={{ apisix_enabled }}
              --set platform={{ kubernetes_platform }}
            {% endif %}
            {% if kubernetes_platform == 'openshift' %}
              --set route.enabled=true
              --set route.host={{ secret_name }}
              --set route.tls.termination=edge
              --set route.tls.insecureEdgeTerminationPolicy=Redirect
              --set ingress.enabled=false
            {% elif ingress_enabled %}
              --set ingress.enabled={{ ingress_enabled }}
              --set ingress.host={{ secret_name }}
              --set ingress.secretname={{ secret_name }}
              --set route.enabled=false
              {% if kubernetes_platform == "eks" %}
              --set aws_certificate_arn={{ aws_certificate_arn | default('') }}
              {% endif %}
            {% endif %}
            {% if deploy_keycloak == 'yes' and apisix_enabled %}
              --set oidc.client_id={{ keycloak_client_id | default('') }}
              --set oidc.client_secret={{ client_secret | default('') }}
            {% endif %}
            {{ helm_proxy_args | default('') }}
          register: helm_upgrade_install_model_deployment_cpu_llama3_2_3b_cpu
          failed_when: helm_upgrade_install_model_deployment_cpu_llama3_2_3b_cpu.rc != 0
        - name: Register Xeon/CPU  Qwen-3-4B model
          import_tasks: register-model-genai-gateway.yml
          vars:
            reg_model_name: "Qwen/Qwen3-4B-Instruct-2507"
            reg_litellm_model: "openai/Qwen/Qwen3-4B-Instruct-2507"
            reg_custom_llm_provider: "openai"
            reg_api_base: "http://vllm-qwen-3-4b-cpu-service.default/v1"
            reg_input_cost_per_token: 0.001
            reg_output_cost_per_token: 0.002
          tags:
            - install-cpu-qwen3-4b
            - install-genai-gateway
          run_once: true
          when:
            - "'install-cpu-qwen3-4b' in ansible_run_tags"
            - "'install-genai-gateway' in ansible_run_tags"
      run_once: true
      tags: install-cpu-qwen3-4b
      when:
        - model_name_list is defined
        - cpu_playbook == 'true'
        - install_true == 'true'
        - "'cpu-qwen3-4b' in (model_name_list | regex_replace(',', ' ') | split())"

    - name: Check if CPU Qwen-2-5-Coder-7B is installed
      ansible.builtin.command:
        cmd: "helm list --filter vllm-qwen-2-5-coder-7b-cpu --short"
      register: helm_release_installed
      ignore_errors: true
      run_once: true
      tags: uninstall-cpu-qwen2-5-coder-7b
    - name: Uninstall Qwen-2-5-Coder-7B
      ansible.builtin.command:
        cmd: "helm uninstall vllm-qwen-2-5-coder-7b-cpu"
      run_once: true
      tags: uninstall-cpu-qwen2-5-coder-7b
      when:
        - model_name_list is defined
        - "'cpu-qwen2-5-coder-7b' in (model_name_list | regex_replace(',', ' ') | split())"
        - uninstall_true == 'true'
        - helm_release_installed.stdout != ""

    - name: Deploy CPU Qwen-2-5-Coder-7B LLM model
      block:
        - name: Delete Ingress resource Qwen-2-5-Coder-7B from default namespace
          tags: install-cpu-qwen2-5-coder-7b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: default
            name: vllm-qwen-2-5-coder-7b-cpu-ingress
            state: absent
        - name: Delete Ingress resource Qwen-2-5-Coder-7B from auth-apisix namespace
          tags: install-cpu-qwen2-5-coder-7b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: auth-apisix
            name: vllm-cpu-qwen-2-5-coder-7b-cpu-ingress
            state: absent
        - name: Deploy CPU based LLM model Qwen-2-5-Coder-7B Installation
          ansible.builtin.command: >-
            helm upgrade --install vllm-qwen-2-5-coder-7b-cpu "{{ remote_helm_charts_base }}/vllm"
            --values "{{ xeon_values_file }}"
            --set LLM_MODEL_ID="Qwen/Qwen2.5-Coder-7B-Instruct"
            --set global.monitoring="{{ vllm_metrics_enabled }}"
            --set svcmonitor.enabled="{{ vllm_metrics_enabled }}"
            --set global.HUGGINGFACEHUB_API_TOKEN={{ hugging_face_token }}
            {% if cpu_playbook == 'true' %}
            --set cpu_balloon_annotation="vllm-balloon"
            --set podLabels.name="vllm"
            --set cpu="{{ optimal_balloon_config.workload_cpus | default(8) }}"
            --set memory="{{ optimal_memory_gb | default(8) }}Gi"
            --set tensor_parallel_size={{ tensor_parallel_size | default(1) }}
            --set pipeline_parallel_size={{ pipeline_parallel_size | default(1) }}
            {% endif %}
            {% if apisix_enabled %}
              --set apisix.enabled={{ apisix_enabled }}
              --set platform={{ kubernetes_platform }}
            {% endif %}
            {% if kubernetes_platform == 'openshift' %}
              --set route.enabled=true
              --set route.host={{ secret_name }}
              --set route.tls.termination=edge
              --set route.tls.insecureEdgeTerminationPolicy=Redirect
              --set ingress.enabled=false
            {% elif ingress_enabled %}
              --set ingress.enabled={{ ingress_enabled }}
              --set ingress.host={{ secret_name }}
              --set ingress.secretname={{ secret_name }}
              --set route.enabled=false
              {% if kubernetes_platform == "eks" %}
              --set aws_certificate_arn={{ aws_certificate_arn | default('') }}
              {% endif %}
            {% endif %}
            {% if deploy_keycloak == 'yes' and apisix_enabled %}
              --set oidc.client_id={{ keycloak_client_id | default('') }}
              --set oidc.client_secret={{ client_secret | default('') }}
            {% endif %}
            {{ helm_proxy_args | default('') }}
          register: helm_upgrade_install_model_deployment_cpu_qwen2_5_coder_7b_cpu
          failed_when: helm_upgrade_install_model_deployment_cpu_qwen2_5_coder_7b_cpu.rc != 0
        - name: Register Xeon/CPU Qwen-2-5-Coder-7B model
          import_tasks: register-model-genai-gateway.yml
          vars:
            reg_model_name: "Qwen/Qwen2.5-Coder-7B-Instruct"
            reg_litellm_model: "openai/Qwen/Qwen2.5-Coder-7B-Instruct"
            reg_custom_llm_provider: "openai"
            reg_api_base: "http://vllm-qwen-2-5-coder-7b-cpu-service.default/v1"
            reg_input_cost_per_token: 0.001
            reg_output_cost_per_token: 0.002
          tags:
            - install-cpu-qwen2-5-coder-7b
            - install-genai-gateway
          run_once: true
          when:
            - "'install-cpu-qwen2-5-coder-7b' in ansible_run_tags"
            - "'install-genai-gateway' in ansible_run_tags"
      run_once: true
      tags: install-cpu-qwen2-5-coder-7b
      when:
        - model_name_list is defined
        - cpu_playbook == 'true'
        - install_true == 'true'
        - "'cpu-qwen2-5-coder-7b' in (model_name_list | regex_replace(',', ' ') | split())"

    - name: Check if CPU Qwen-2-5-Coder-14B is installed
      ansible.builtin.command:
        cmd: "helm list --filter vllm-qwen-2-5-coder-14b-cpu --short"
      register: helm_release_installed
      ignore_errors: true
      run_once: true
      tags: uninstall-cpu-qwen2-5-coder-14b
    - name: Uninstall Qwen-2-5-Coder-14B
      ansible.builtin.command:
        cmd: "helm uninstall vllm-qwen-2-5-coder-14b-cpu"
      run_once: true
      tags: uninstall-cpu-qwen2-5-coder-14b
      when:
        - model_name_list is defined
        - "'cpu-qwen2-5-coder-14b' in (model_name_list | regex_replace(',', ' ') | split())"
        - uninstall_true == 'true'
        - helm_release_installed.stdout != ""

    - name: Deploy CPU Qwen-2-5-Coder-14B LLM model
      block:
        - name: Delete Ingress resource Qwen-2-5-Coder-14B from default namespace
          tags: install-cpu-qwen2-5-coder-14b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: default
            name: vllm-qwen-2-5-coder-14b-cpu-ingress
            state: absent
        - name: Delete Ingress resource Qwen-2-5-Coder-14B from auth-apisix namespace
          tags: install-cpu-qwen2-5-coder-14b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: auth-apisix
            name: vllm-cpu-qwen-2-5-coder-14b-cpu-ingress
            state: absent
        - name: Deploy CPU based LLM model Qwen-2-5-Coder-14B Installation
          ansible.builtin.command: >-
            helm upgrade --install vllm-qwen-2-5-coder-14b-cpu "{{ remote_helm_charts_base }}/vllm"
            --values "{{ xeon_values_file }}"
            --set LLM_MODEL_ID="Qwen/Qwen2.5-Coder-14B-Instruct"
            --set chatTemplateConfigMap.enabled=true
            --set clearHFLocks.enabled=true
            --set podSecurityContext.runAsUser=0
            --set podSecurityContext.fsGroup=0
            --set securityContext.runAsUser=0
            --set securityContext.runAsNonRoot=false
            --set global.monitoring="{{ vllm_metrics_enabled }}"
            --set svcmonitor.enabled="{{ vllm_metrics_enabled }}"
            --set global.HUGGINGFACEHUB_API_TOKEN={{ hugging_face_token }}
            {% if cpu_playbook == 'true' %}
            --set cpu_balloon_annotation="vllm-balloon"
            --set podLabels.name="vllm"
            --set cpu="{{ optimal_balloon_config.workload_cpus | default(8) }}"
            --set memory="{{ optimal_memory_gb | default(8) }}Gi"
            --set tensor_parallel_size={{ tensor_parallel_size | default(1) }}
            --set pipeline_parallel_size={{ pipeline_parallel_size | default(1) }}
            {% endif %}
            {% if apisix_enabled %}
              --set apisix.enabled={{ apisix_enabled }}
              --set platform={{ kubernetes_platform }}
            {% endif %}
            {% if kubernetes_platform == 'openshift' %}
              --set route.enabled=true
              --set route.host={{ secret_name }}
              --set route.tls.termination=edge
              --set route.tls.insecureEdgeTerminationPolicy=Redirect
              --set ingress.enabled=false
            {% elif ingress_enabled %}
              --set ingress.enabled={{ ingress_enabled }}
              --set ingress.host={{ secret_name }}
              --set ingress.secretname={{ secret_name }}
              --set route.enabled=false
              {% if kubernetes_platform == "eks" %}
              --set aws_certificate_arn={{ aws_certificate_arn | default('') }}
              {% endif %}
            {% endif %}
            {% if deploy_keycloak == 'yes' and apisix_enabled %}
              --set oidc.client_id={{ keycloak_client_id | default('') }}
              --set oidc.client_secret={{ client_secret | default('') }}
            {% endif %}
            {{ helm_proxy_args | default('') }}
          register: helm_upgrade_install_model_deployment_cpu_qwen2_5_coder_14b_cpu
          failed_when: helm_upgrade_install_model_deployment_cpu_qwen2_5_coder_14b_cpu.rc != 0
        - name: Register Xeon/CPU Qwen-2-5-Coder-14B model
          import_tasks: register-model-genai-gateway.yml
          vars:
            reg_model_name: "Qwen/Qwen2.5-Coder-14B-Instruct"
            reg_litellm_model: "openai/Qwen/Qwen2.5-Coder-14B-Instruct"
            reg_custom_llm_provider: "openai"
            reg_api_base: "http://vllm-qwen-2-5-coder-14b-cpu-service.default/v1"
            reg_input_cost_per_token: 0.001
            reg_output_cost_per_token: 0.002
          tags:
            - install-cpu-qwen2-5-coder-14b
            - install-genai-gateway
          run_once: true
          when:
            - "'install-cpu-qwen2-5-coder-14b' in ansible_run_tags"
            - "'install-genai-gateway' in ansible_run_tags"
      run_once: true
      tags: install-cpu-qwen2-5-coder-14b
      when:
        - model_name_list is defined
        - cpu_playbook == 'true'
        - install_true == 'true'
        - "'cpu-qwen2-5-coder-14b' in (model_name_list | regex_replace(',', ' ') | split())"

    - name: Check if CPU Qwen3-Coder-30B is installed
      ansible.builtin.command:
        cmd: "helm list --filter vllm-qwen3-coder-30b-cpu --short"
      register: helm_release_installed
      ignore_errors: true
      run_once: true
      tags: uninstall-cpu-qwen3-coder-30b
    - name: Uninstall Qwen3-Coder-30B
      ansible.builtin.command:
        cmd: "helm uninstall vllm-qwen3-coder-30b-cpu"
      run_once: true
      tags: uninstall-cpu-qwen3-coder-30b
      when:
        - model_name_list is defined
        - "'cpu-qwen3-coder-30b' in (model_name_list | regex_replace(',', ' ') | split())"
        - uninstall_true == 'true'
        - helm_release_installed.stdout != ""

    - name: Deploy CPU Qwen3-Coder-30B LLM model
      block:
        - name: Delete Ingress resource Qwen3-Coder-30B from default namespace
          tags: install-cpu-qwen3-coder-30b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: default
            name: vllm-qwen3-coder-30b-cpu-ingress
            state: absent
        - name: Delete Ingress resource Qwen3-Coder-30B from auth-apisix namespace
          tags: install-cpu-qwen3-coder-30b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: auth-apisix
            name: vllm-cpu-qwen3-coder-30b-cpu-ingress
            state: absent
        - name: Deploy CPU based LLM model Qwen3-Coder-30B Installation
          ansible.builtin.command: >-
            helm upgrade --install vllm-qwen3-coder-30b-cpu "{{ remote_helm_charts_base }}/vllm"
            --values "{{ xeon_values_file }}"
            --set LLM_MODEL_ID="Qwen/Qwen3-Coder-30B-A3B-Instruct"
            --set chatTemplateConfigMap.enabled=true
            --set hostIPC=true
            --set clearHFLocks.enabled=true
            --set podSecurityContext.runAsUser=0
            --set podSecurityContext.fsGroup=0
            --set securityContext.runAsUser=0
            --set securityContext.runAsNonRoot=false
            --set global.monitoring="{{ vllm_metrics_enabled }}"
            --set svcmonitor.enabled="{{ vllm_metrics_enabled }}"
            --set global.HUGGINGFACEHUB_API_TOKEN={{ hugging_face_token }}
            {% if cpu_playbook == 'true' %}
            --set cpu_balloon_annotation="vllm-balloon"
            --set podLabels.name="vllm"
            --set cpu="{{ optimal_balloon_config.workload_cpus | default(8) }}"
            --set memory="{{ optimal_memory_gb | default(8) }}Gi"
            --set tensor_parallel_size={{ tensor_parallel_size | default(1) }}
            --set pipeline_parallel_size={{ pipeline_parallel_size | default(1) }}
            {% endif %}
            {% if apisix_enabled %}
              --set apisix.enabled={{ apisix_enabled }}
              --set platform={{ kubernetes_platform }}
            {% endif %}
            {% if kubernetes_platform == 'openshift' %}
              --set route.enabled=true
              --set route.host={{ secret_name }}
              --set route.tls.termination=edge
              --set route.tls.insecureEdgeTerminationPolicy=Redirect
              --set ingress.enabled=false
            {% elif ingress_enabled %}
              --set ingress.enabled={{ ingress_enabled }}
              --set ingress.host={{ secret_name }}
              --set ingress.secretname={{ secret_name }}
              --set route.enabled=false
              {% if kubernetes_platform == "eks" %}
              --set aws_certificate_arn={{ aws_certificate_arn | default('') }}
              {% endif %}
            {% endif %}
            {% if deploy_keycloak == 'yes' and apisix_enabled %}
              --set oidc.client_id={{ keycloak_client_id | default('') }}
              --set oidc.client_secret={{ client_secret | default('') }}
            {% endif %}
            {{ helm_proxy_args | default('') }}
          register: helm_upgrade_install_model_deployment_cpu_qwen3_coder_30b_cpu
          failed_when: helm_upgrade_install_model_deployment_cpu_qwen3_coder_30b_cpu.rc != 0
        - name: Register Xeon/CPU Qwen3-Coder-30B model
          import_tasks: register-model-genai-gateway.yml
          vars:
            reg_model_name: "Qwen/Qwen3-Coder-30B-A3B-Instruct"
            reg_litellm_model: "hosted_vllm/Qwen/Qwen3-Coder-30B-A3B-Instruct"
            reg_custom_llm_provider: "hosted_vllm"
            reg_api_base: "http://vllm-qwen3-coder-30b-cpu-service.default/v1"
            reg_input_cost_per_token: 0.001
            reg_output_cost_per_token: 0.002
          tags:
            - install-cpu-qwen3-coder-30b
            - install-genai-gateway
          run_once: true
          when:
            - "'install-cpu-qwen3-coder-30b' in ansible_run_tags"
            - "'install-genai-gateway' in ansible_run_tags"
      run_once: true
      tags: install-cpu-qwen3-coder-30b
      when:
        - model_name_list is defined
        - cpu_playbook == 'true'
        - install_true == 'true'
        - "'cpu-qwen3-coder-30b' in (model_name_list | regex_replace(',', ' ') | split())"


    - name: Check if CPU vllm-whisper-small Model is deployed
      ansible.builtin.command:
        cmd: "helm list --filter vllm-whisper-small-cpu --short"
      register: helm_release_installed
      ignore_errors: true
      run_once: true
      tags: uninstall-cpu-whisper-small
    - name: Uninstall CPU vllm-whisper-small Model
      ansible.builtin.command:
        cmd: "helm uninstall vllm-whisper-small-cpu"
      run_once: true
      tags: uninstall-cpu-whisper-small
      when:
        - model_name_list is defined
        - "'cpu-whisper-small' in (model_name_list | regex_replace(',', ' ') | split())"
        - uninstall_true == 'true'
        - helm_release_installed.stdout != ""

    - name: Deploy CPU Whisper Small ASR model
      block:
        - name: Delete Ingress resource CPU Whisper Small from default namespace
          tags: install-cpu-whisper-small
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: default
            name: vllm-whisper-small-cpu-ingress
            state: absent
        - name: Delete Ingress resource CPU Whisper Small from auth-apisix namespace
          tags: install-cpu-whisper-small
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: auth-apisix
            name: vllm-whisper-small-cpu-ingress
            state: absent
        - name: Deploy CPU based ASR model Whisper Small Installation
          ansible.builtin.command: >-
            helm upgrade --install vllm-whisper-small-cpu "{{ remote_helm_charts_base }}/vllm"
            --values "{{ xeon_values_file }}"
            --set LLM_MODEL_ID="openai/whisper-small"
            --set global.monitoring="{{ vllm_metrics_enabled }}"
            --set svcmonitor.enabled="{{ vllm_metrics_enabled }}"
            --set global.HUGGINGFACEHUB_API_TOKEN={{ hugging_face_token }}
            {% if cpu_playbook == 'true' %}
            --set cpu_balloon_annotation="vllm-balloon"
            --set podLabels.name="vllm"
            --set cpu="{{ optimal_balloon_config.workload_cpus | default(8) }}"
            --set memory="{{ optimal_memory_gb | default(8) }}Gi"
            --set tensor_parallel_size={{ tensor_parallel_size | default(1) }}
            --set pipeline_parallel_size={{ pipeline_parallel_size | default(1) }}
            {% endif %}
            {% if apisix_enabled %}
              --set apisix.enabled={{ apisix_enabled }}
              --set platform={{ kubernetes_platform }}
            {% endif %}
            {% if kubernetes_platform == 'openshift' %}
              --set route.enabled=true
              --set route.host={{ secret_name }}
              --set route.tls.termination=edge
              --set route.tls.insecureEdgeTerminationPolicy=Redirect
              --set ingress.enabled=false
            {% elif ingress_enabled %}
              --set ingress.enabled={{ ingress_enabled }}
              --set ingress.host={{ secret_name }}
              --set ingress.secretname={{ secret_name }}
              --set route.enabled=false
              {% if kubernetes_platform == "eks" %}
              --set aws_certificate_arn={{ aws_certificate_arn | default('') }}
              {% endif %}
            {% endif %}
            {% if deploy_keycloak == 'yes' and apisix_enabled %}
              --set oidc.client_id={{ keycloak_client_id | default('') }}
              --set oidc.client_secret={{ client_secret | default('') }}
            {% endif %}
            {{ helm_proxy_args | default('') }}
          register: helm_upgrade_install_model_deployment_cpu_whisper_small
          failed_when: helm_upgrade_install_model_deployment_cpu_whisper_small.rc != 0
        - name: Register Xeon/CPU Whisper Small model
          import_tasks: register-model-genai-gateway.yml
          vars:
            reg_model_name: "openai/whisper-small"
            reg_litellm_model: "openai/openai/whisper-small"
            reg_custom_llm_provider: "openai"
            reg_api_base: "http://vllm-whisper-small-cpu-service.default/v1"
            reg_input_cost_per_token: 0.001
            reg_output_cost_per_token: 0.002
          tags:
            - install-cpu-whisper-small
            - install-genai-gateway
          run_once: true
          when:
            - "'install-cpu-whisper-small' in ansible_run_tags"
            - "'install-genai-gateway' in ansible_run_tags"
      run_once: true
      tags: install-cpu-whisper-small
      when:
        - model_name_list is defined
        - cpu_playbook == 'true'
        - install_true == 'true'
        - "'cpu-whisper-small' in (model_name_list | regex_replace(',', ' ') | split())"

    - name: Check if CPU TEI Embedding is installed
      ansible.builtin.command:
        cmd: "helm list --filter vllm-bge-base-en-cpu --short"
      register: helm_release_installed
      ignore_errors: true
      run_once: true
      tags: uninstall-cpu-bge-base-en
    - name: Uninstall CPU TEI Embedding
      ansible.builtin.command:
        cmd: "helm uninstall vllm-bge-base-en-cpu"
      run_once: true
      tags: uninstall-cpu-bge-base-en
      when:
        - model_name_list is defined
        - "'cpu-bge-base-en' in (model_name_list | regex_replace(',', ' ') | split())"
        - uninstall_true == 'true'
        - helm_release_installed.stdout != ""

    - name: Deploy CPU TEI Embedding model
      block:
        - name: Delete Ingress resource CPU TEI Embedding from default namespace
          tags: install-cpu-bge-base-en
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: default
            name: vllm-bge-base-en-cpu-ingress
            state: absent
        - name: Delete Ingress resource CPU TEI Embedding from auth-apisix namespace
          tags: install-cpu-bge-base-en
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: auth-apisix
            name: vllm-bge-base-en-cpu-ingress
            state: absent
        - name: Deploy CPU Embedding model BAAI/bge-base-en-v1.5 Installation
          ansible.builtin.command: >-
            helm upgrade --install vllm-bge-base-en-cpu "{{ remote_helm_charts_base }}/vllm"
            --values "{{ remote_helm_charts_base }}/vllm/embedding-values.yaml"
            --set EMBEDDING_MODEL_ID="BAAI/bge-base-en-v1.5"
            --set global.HUGGINGFACEHUB_API_TOKEN={{ hugging_face_token }}
            --set tensor_parallel_size=1
            {% if apisix_enabled %}
              --set apisix.enabled={{ apisix_enabled }}
              --set platform={{ kubernetes_platform }}
            {% endif %}
            {% if kubernetes_platform == 'openshift' %}
              --set route.enabled=true
              --set route.host={{ secret_name }}
              --set route.tls.termination=edge
              --set route.tls.insecureEdgeTerminationPolicy=Redirect
              --set ingress.enabled=false
            {% elif ingress_enabled %}
              --set ingress.enabled={{ ingress_enabled }}
              --set ingress.host={{ secret_name }}
              --set ingress.secretname={{ secret_name }}
              --set route.enabled=false
              {% if kubernetes_platform == "eks" %}
              --set aws_certificate_arn={{ aws_certificate_arn | default('') }}
              {% endif %}
            {% endif %}
            {% if deploy_keycloak == 'yes' and apisix_enabled %}
              --set oidc.client_id={{ keycloak_client_id | default('') }}
              --set oidc.client_secret={{ client_secret | default('') }}
            {% endif %}
            --set teirerank.namespace=default
            {{ helm_proxy_args | default('') }}
          register: helm_upgrade_install_model_deployment_cpu_bge_base_en
          failed_when: helm_upgrade_install_model_deployment_cpu_bge_base_en.rc != 0
          tags: install-cpu-bge-base-en
        - name: Register Xeon/CPU TEI Embedding model
          import_tasks: register-model-genai-gateway.yml
          vars:
            reg_model_name: "BAAI/bge-base-en-v1.5"
            reg_litellm_model: "hosted_vllm/BAAI/bge-base-en-v1.5"
            reg_custom_llm_provider: "hosted_vllm"
            reg_api_base: "http://vllm-bge-base-en-cpu-service.default/v1"
            reg_input_cost_per_token: 0.001
            reg_output_cost_per_token: 0.002
            reg_model_mode: "embedding"
          tags:
            - install-cpu-bge-base-en
            - install-genai-gateway
          run_once: true
          when:
            - "'install-cpu-bge-base-en' in ansible_run_tags"
            - "'install-genai-gateway' in ansible_run_tags"
      run_once: true
      tags: install-cpu-bge-base-en
      when:
        - model_name_list is defined
        - cpu_playbook == 'true'
        - install_true == 'true'
        - "'cpu-bge-base-en' in (model_name_list | regex_replace(',', ' ') | split())"

    - name: Check if CPU TEI Reranker is installed
      ansible.builtin.command:
        cmd: "helm list --filter vllm-bge-reranker-base-cpu --short"
      register: helm_release_installed
      ignore_errors: true
      run_once: true
      tags: uninstall-cpu-bge-reranker-base
    - name: Uninstall CPU TEI Reranker
      ansible.builtin.command:
        cmd: "helm uninstall vllm-bge-reranker-base-cpu"
      run_once: true
      tags: uninstall-cpu-bge-reranker-base
      when:
        - model_name_list is defined
        - "'cpu-bge-reranker-base' in (model_name_list | regex_replace(',', ' ') | split())"
        - uninstall_true == 'true'
        - helm_release_installed.stdout != ""

    - name: Deploy CPU TEI Reranker model
      block:
        - name: Delete Ingress resource CPU TEI Reranker from default namespace
          tags: install-cpu-bge-reranker-base
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: default
            name: vllm-bge-reranker-base-cpu-ingress
            state: absent
        - name: Delete Ingress resource CPU TEI Reranker from auth-apisix namespace
          tags: install-cpu-bge-reranker-base
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: auth-apisix
            name: vllm-bge-reranker-base-cpu-ingress
            state: absent
        - name: Deploy CPU Reranker model BAAI/bge-reranker-base Installation
          ansible.builtin.command: >-
            helm upgrade --install vllm-bge-reranker-base-cpu "{{ remote_helm_charts_base }}/vllm"
            --values "{{ remote_helm_charts_base }}/vllm/reranker-values.yaml"
            --set RERANK_MODEL_ID="BAAI/bge-reranker-base"
            --set global.HUGGINGFACEHUB_API_TOKEN={{ hugging_face_token }}
            --set tensor_parallel_size=1
            {% if apisix_enabled %}
              --set apisix.enabled={{ apisix_enabled }}
              --set platform={{ kubernetes_platform }}
            {% endif %}
            {% if kubernetes_platform == 'openshift' %}
              --set route.enabled=true
              --set route.host={{ secret_name }}
              --set route.tls.termination=edge
              --set route.tls.insecureEdgeTerminationPolicy=Redirect
              --set ingress.enabled=false
            {% elif ingress_enabled %}
              --set ingress.enabled={{ ingress_enabled }}
              --set ingress.host={{ secret_name }}
              --set ingress.secretname={{ secret_name }}
              --set route.enabled=false
              {% if kubernetes_platform == "eks" %}
              --set aws_certificate_arn={{ aws_certificate_arn | default('') }}
              {% endif %}
            {% endif %}
            {% if deploy_keycloak == 'yes' and apisix_enabled %}
              --set oidc.client_id={{ keycloak_client_id | default('') }}
              --set oidc.client_secret={{ client_secret | default('') }}
            {% endif %}
            --set teirerank.namespace=default
            {{ helm_proxy_args | default('') }}
          register: helm_upgrade_install_model_deployment_cpu_bge_reranker_base
          failed_when: helm_upgrade_install_model_deployment_cpu_bge_reranker_base.rc != 0
          tags: install-cpu-bge-reranker-base
        - name: Register Xeon/CPU TEI Reranker model
          import_tasks: register-model-genai-gateway.yml
          vars:
            reg_model_name: "BAAI/bge-reranker-base"
            reg_litellm_model: "hosted_vllm/BAAI/bge-reranker-base"
            reg_custom_llm_provider: "hosted_vllm"
            reg_api_base: "http://vllm-bge-reranker-base-cpu-service.default/v1"
            reg_input_cost_per_token: 0.001
            reg_output_cost_per_token: 0.002
            reg_model_mode: "rerank"
          tags:
            - install-cpu-bge-reranker-base
            - install-genai-gateway
          run_once: true
          when:
            - "'install-cpu-bge-reranker-base' in ansible_run_tags"
            - "'install-genai-gateway' in ansible_run_tags"
      run_once: true
      tags: install-cpu-bge-reranker-base
      when:
        - model_name_list is defined
        - cpu_playbook == 'true'
        - install_true == 'true'
        - "'cpu-bge-reranker-base' in (model_name_list | regex_replace(',', ' ') | split())"

    - name: Check if CPU Qwen3-30B-A3B is installed
      ansible.builtin.command:
        cmd: "helm list --filter vllm-qwen3-30b-a3b-cpu --short"
      register: helm_release_installed
      ignore_errors: true
      run_once: true
      tags: uninstall-cpu-qwen3-30b-a3b
    - name: Uninstall CPU Qwen3-30B-A3B
      ansible.builtin.command:
        cmd: "helm uninstall vllm-qwen3-30b-a3b-cpu"
      run_once: true
      tags: uninstall-cpu-qwen3-30b-a3b
      when:
        - model_name_list is defined
        - "'cpu-qwen3-30b-a3b' in (model_name_list | regex_replace(',', ' ') | split())"
        - uninstall_true == 'true'
        - helm_release_installed.stdout != ""

    - name: Deploy CPU Qwen3-30B-A3B-Instruct-2507 LLM model
      block:
        - name: Delete Ingress resource CPU Qwen3-30B-A3B from default namespace
          tags: install-cpu-qwen3-30b-a3b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: default
            name: vllm-qwen3-30b-a3b-cpu-ingress
            state: absent
        - name: Delete Ingress resource CPU Qwen3-30B-A3B from auth-apisix namespace
          tags: install-cpu-qwen3-30b-a3b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: auth-apisix
            name: vllm-qwen3-30b-a3b-cpu-ingress
            state: absent
        - name: Deploy CPU based LLM model Qwen3-30B-A3B-Instruct-2507 Installation
          ansible.builtin.command: >-
            helm upgrade --install vllm-qwen3-30b-a3b-cpu "{{ remote_helm_charts_base }}/vllm"
            --values "{{ xeon_values_file }}"
            --set LLM_MODEL_ID="Qwen/Qwen3-30B-A3B-Instruct-2507"
            --set chatTemplateConfigMap.enabled=true
            --set hostIPC=true
            --set global.monitoring="{{ vllm_metrics_enabled }}"
            --set svcmonitor.enabled="{{ vllm_metrics_enabled }}"
            --set global.HUGGINGFACEHUB_API_TOKEN={{ hugging_face_token }}
            {% if cpu_playbook == 'true' %}
            --set cpu_balloon_annotation="vllm-balloon"
            --set podLabels.name="vllm"
            --set cpu="{{ optimal_balloon_config.workload_cpus | default(48) }}"
            --set memory="{{ optimal_memory_gb | default(128) }}Gi"
            --set tensor_parallel_size={{ tensor_parallel_size | default(1) }}
            --set pipeline_parallel_size={{ pipeline_parallel_size | default(1) }}
            {% endif %}
            {% if apisix_enabled %}
              --set apisix.enabled={{ apisix_enabled }}
              --set platform={{ kubernetes_platform }}
            {% endif %}
            {% if kubernetes_platform == 'openshift' %}
              --set route.enabled=true
              --set route.host={{ secret_name }}
              --set route.tls.termination=edge
              --set route.tls.insecureEdgeTerminationPolicy=Redirect
              --set ingress.enabled=false
            {% elif ingress_enabled %}
              --set ingress.enabled={{ ingress_enabled }}
              --set ingress.host={{ secret_name }}
              --set ingress.secretname={{ secret_name }}
              --set route.enabled=false
              {% if kubernetes_platform == "eks" %}
              --set aws_certificate_arn={{ aws_certificate_arn | default('') }}
              {% endif %}
            {% endif %}
            {% if deploy_keycloak == 'yes' and apisix_enabled %}
              --set oidc.client_id={{ keycloak_client_id | default('') }}
              --set oidc.client_secret={{ client_secret | default('') }}
            {% endif %}
            {{ helm_proxy_args | default('') }}
          register: helm_upgrade_install_model_deployment_cpu_qwen3_30b_a3b
          failed_when: helm_upgrade_install_model_deployment_cpu_qwen3_30b_a3b.rc != 0
        - name: Register Xeon/CPU Qwen3-30B-A3B-Instruct-2507 model
          import_tasks: register-model-genai-gateway.yml
          vars:
            reg_model_name: "Qwen/Qwen3-30B-A3B-Instruct-2507"
            reg_litellm_model: "hosted_vllm/Qwen/Qwen3-30B-A3B-Instruct-2507"
            reg_custom_llm_provider: "hosted_vllm"
            reg_api_base: "http://vllm-qwen3-30b-a3b-cpu-service.default/v1"
            reg_input_cost_per_token: 0.001
            reg_output_cost_per_token: 0.002
          tags:
            - install-cpu-qwen3-30b-a3b
            - install-genai-gateway
          run_once: true
          when:
            - "'install-cpu-qwen3-30b-a3b' in ansible_run_tags"
            - "'install-genai-gateway' in ansible_run_tags"
      run_once: true
      tags: install-cpu-qwen3-30b-a3b
      when:
        - model_name_list is defined
        - cpu_playbook == 'true'
        - install_true == 'true'
        - "'cpu-qwen3-30b-a3b' in (model_name_list | regex_replace(',', ' ') | split())"

    - name: Check if CPU Gemma-4-26B is installed
      ansible.builtin.command:
        cmd: "helm list --filter vllm-gemma4-26b-a4b-cpu --short"
      register: helm_release_installed
      ignore_errors: true
      run_once: true
      tags: uninstall-cpu-gemma4-26b-a4b
    - name: Uninstall CPU Gemma-4-26B
      ansible.builtin.command:
        cmd: "helm uninstall vllm-gemma4-26b-a4b-cpu"
      run_once: true
      tags: uninstall-cpu-gemma4-26b-a4b
      when:
        - model_name_list is defined
        - "'cpu-gemma4-26b-a4b' in (model_name_list | regex_replace(',', ' ') | split())"
        - uninstall_true == 'true'
        - helm_release_installed.stdout != ""

    - name: Deploy CPU gemma-4-26B-A4B-it LLM model
      block:
        - name: Delete Ingress resource CPU Gemma-4-26B from default namespace
          tags: install-cpu-gemma4-26b-a4b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: default
            name: vllm-gemma4-26b-a4b-cpu-ingress
            state: absent
        - name: Delete Ingress resource CPU Gemma-4-26B from auth-apisix namespace
          tags: install-cpu-gemma4-26b-a4b
          ignore_errors: yes
          kubernetes.core.k8s:
            kind: Ingress
            namespace: auth-apisix
            name: vllm-gemma4-26b-a4b-cpu-ingress
            state: absent
        - name: Deploy CPU based LLM model gemma-4-26B-A4B-it Installation
          ansible.builtin.command: >-
            helm upgrade --install vllm-gemma4-26b-a4b-cpu "{{ remote_helm_charts_base }}/vllm"
            --values "{{ xeon_values_file }}"
            --set LLM_MODEL_ID="google/gemma-4-26B-A4B-it"
            --set global.monitoring="{{ vllm_metrics_enabled }}"
            --set svcmonitor.enabled="{{ vllm_metrics_enabled }}"
            --set global.HUGGINGFACEHUB_API_TOKEN={{ hugging_face_token }}
            {% if cpu_playbook == 'true' %}
            --set cpu_balloon_annotation="vllm-balloon"
            --set podLabels.name="vllm"
            --set cpu="{{ optimal_balloon_config.workload_cpus | default(8) }}"
            --set memory="{{ optimal_memory_gb | default(64) }}Gi"
            --set tensor_parallel_size={{ tensor_parallel_size | default(1) }}
            --set pipeline_parallel_size={{ pipeline_parallel_size | default(1) }}
            {% endif %}
            {% if apisix_enabled %}
              --set apisix.enabled={{ apisix_enabled }}
              --set platform={{ kubernetes_platform }}
            {% endif %}
            {% if kubernetes_platform == 'openshift' %}
              --set route.enabled=true
              --set route.host={{ secret_name }}
              --set route.tls.termination=edge
              --set route.tls.insecureEdgeTerminationPolicy=Redirect
              --set ingress.enabled=false
            {% elif ingress_enabled %}
              --set ingress.enabled={{ ingress_enabled }}
              --set ingress.host={{ secret_name }}
              --set ingress.secretname={{ secret_name }}
              --set route.enabled=false
              {% if kubernetes_platform == "eks" %}
              --set aws_certificate_arn={{ aws_certificate_arn | default('') }}
              {% endif %}
            {% endif %}
            {% if deploy_keycloak == 'yes' and apisix_enabled %}
              --set oidc.client_id={{ keycloak_client_id | default('') }}
              --set oidc.client_secret={{ client_secret | default('') }}
            {% endif %}
            {{ helm_proxy_args | default('') }}
          register: helm_upgrade_install_model_deployment_cpu_gemma4_26b
          failed_when: helm_upgrade_install_model_deployment_cpu_gemma4_26b.rc != 0
        - name: Register Xeon/CPU Gemma-4-26B model
          import_tasks: register-model-genai-gateway.yml
          vars:
            reg_model_name: "google/gemma-4-26B-A4B-it"
            reg_litellm_model: "openai/google/gemma-4-26B-A4B-it"
            reg_custom_llm_provider: "openai"
            reg_api_base: "http://vllm-gemma4-26b-a4b-cpu-service.default/v1"
            reg_input_cost_per_token: 0.001
            reg_output_cost_per_token: 0.002
          tags:
            - install-cpu-gemma4-26b-a4b
          when:
            - "'install-cpu-gemma4-26b-a4b' in ansible_run_tags"
            - "'install-genai-gateway' in ansible_run_tags"
      run_once: true
      tags: install-cpu-gemma4-26b-a4b
      when:
        - model_name_list is defined
        - cpu_playbook == 'true'
        - install_true == 'true'
        - "'cpu-gemma4-26b-a4b' in (model_name_list | regex_replace(',', ' ') | split())"

    - name: List of Models to be Installed
      tags: always
      run_once: true
      debug:
        var: model_name_list

    - name: List the Installed Inference Models
      ansible.builtin.shell:
        cmd: "helm list --short | grep 'vllm-'"
      register: inference_models
      when: list_model_true == 'true'
    - name: Print Installed Models in Comma Separated Format
      ansible.builtin.debug:
        msg: "Installed Models: {{ inference_models.stdout_lines | join(', ') }}"
      when: inference_models.stdout_lines is defined and inference_models.stdout_lines | length > 0

    - name: Clean up remote dependencies directory
      tags: always
      ansible.builtin.file:
        path: "{{ remote_helm_charts_base }}"
        state: absent