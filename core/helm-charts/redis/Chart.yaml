apiVersion: v2
name: redis
description: Standalone Redis Stack (with RediSearch) for shared use by agentic workloads
type: application
version: 0.1.0
appVersion: "7.4.0"
keywords:
  - redis
  - memory
  - vector-search
# redis-stack-server is vendored locally in charts/redis-stack-server/
# (patched to support affinity + tolerations for node scheduling).
# Do NOT add it back as a Helm dependency — helm dep update would overwrite the patch.
